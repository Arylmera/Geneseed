# Normalize line endings so the update preflight's clean-tree check never sees spurious
# CRLF/exec-bit churn (rituals/_update.py runs `git status` with core.autocrlf=false).
* text=auto eol=lf
# Windows launchers must stay CRLF for cmd.exe / PowerShell.
*.cmd text eol=crlf
*.ps1 text eol=crlf
