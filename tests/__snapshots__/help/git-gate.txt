usage: harness git-gate [-h] [--root ROOT]

options:
  -h, --help   show this help message and exit
  --root ROOT  the install's own dir (set by the emitted hook); enables the
               sovereign-repo bypass from <root>/excludes.json
